export const ROLES = {
  ADMIN: 'admin'
}
export class User {
  $key: string;
  role: string;
  displayName: string;
  email: string;
  photoURL: string;
  createdDate: string;
  reservations: string;
}
